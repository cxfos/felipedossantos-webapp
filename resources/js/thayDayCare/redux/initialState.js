const initialState = {
  appConfig: {
    appWindow: {
      height: window.innerHeight || 0,
      width: window.innerWidth || 0
    },
    title: 'Thay Day Care',
    description: 'This is Thay child home day care webapp page'
  }
};

export default initialState;
